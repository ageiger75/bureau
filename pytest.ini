[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
# `.` dans le chemin d'import : les tests importent `app` et `seed` comme le fait uvicorn.
pythonpath = .
filterwarnings =
    error::DeprecationWarning:app.*
